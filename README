# Web Calculator

Этот проект представляет собой распределенную систему калькулятора, состоящую из двух основных компонентов:

1. **Оркестратор** — сервер, который принимает арифметическое выражение, переводит его в набор последовательных задач и обеспечивает порядок их выполнения.
2. **Агент** — вычислитель, который получает задачу от оркестратора, выполняет вычисления и возвращает результат обратно серверу.

## Структура проекта

- **Оркестратор (Server)**: Управляет задачами, распределяет их между агентами и контролирует их выполнение.
- **Агент (Agent)**: Получает задачи от оркестратора, выполняет вычисления и возвращает результат.

## Архитектура

1. **Оркестратор**:
   - Принимает арифметическое выражение от клиента.
   - Разделяет выражение на отдельные подзадачи (например, выполнение операций).
   - Обрабатывает очередь задач и отправляет их агентам.
   - Сохраняет результаты выполнения и возвращает их клиенту.
   
2. **Агент**:
   - Получает задачу от оркестратора.
   - Выполняет вычисления (например, арифметические операции).
   - Отправляет результат обратно оркестратору.

## Технологии

- **Go**: Язык программирования для реализации серверной логики и вычислений.
- **gRPC**: Протокол для обмена сообщениями между оркестратором и агентами.
- **JSON**: Формат для обмена данными между сервером и клиентом.

## Запуск проекта

### 1. Запуск оркестратора
1. Для начала необходимо клонировать репозиторий проекта себе на компьютер, нужно использовать эту команду
